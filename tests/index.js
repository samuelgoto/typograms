const assert = require("assert");

describe("Typograms", function() {

  it("", () => {
  });
  
  function assertThat(x) {
    return {
      equalsTo(y) {
        assert.deepEqual(x, y);
      }
    }
  }
});
